import {guestDb} from "@/db/guestbook";
export const dynamic="force-dynamic";
const json=(data:unknown,status=200)=>Response.json(data,{status,headers:{"Cache-Control":"no-store"}});
export async function GET(request:Request){try{const db=guestDb();const before=new URL(request.url).searchParams.get("before");let rows;if(before){rows=await db.prepare("SELECT id,name,message,created_at FROM wishes WHERE (created_at,id) < (SELECT created_at,id FROM wishes WHERE id=?) ORDER BY created_at DESC,id DESC LIMIT 21").bind(before).all()}else rows=await db.prepare("SELECT id,name,message,created_at FROM wishes ORDER BY created_at DESC,id DESC LIMIT 21").all();return json({entries:rows.results.slice(0,20),more:rows.results.length>20})}catch(error){console.error("Guestbook read failed",error);return json({error:"The guestbook is temporarily unavailable."},503)}}
async function forwardEmail(name:string,message:string,id:string,url:string){try{const r=await fetch("https://formsubmit.co/ajax/ahmed.qandeel96@gmail.com",{method:"POST",headers:{"Content-Type":"application/json","Accept":"application/json"},body:JSON.stringify({name,message,reference:id,_subject:"A&Q Wedding — A new guestbook message",_template:"table",_url:new URL("/invitation",url).href}),signal:AbortSignal.timeout(12000)});const d=await r.json() as {success?:boolean|string};return r.ok&&(d.success===true||d.success==="true")}catch(error){console.error("Guestbook email forwarding unavailable");return false}}
export async function POST(request:Request){try{const origin=request.headers.get("origin");if(origin&&origin!==new URL(request.url).origin)return json({error:"Please submit from the invitation page."},403);const raw=await request.text();if(raw.length>12000)return json({error:"Your message is too long."},400);let p;try{p=JSON.parse(raw)}catch{return json({error:"Please check your message."},400)}if(typeof p.name!=="string"||typeof p.message!=="string"||typeof p.id!=="string")return json({error:"Please include your name and message."},400);const name=p.name.trim(),message=p.message.trim();if(name.length<1||name.length>80||message.length<3||message.length>1500||!/^[0-9a-f-]{36}$/i.test(p.id)||p.website)return json({error:"Please enter a name and a message of 3–1,500 characters."},400);const db=guestDb();const existing=await db.prepare("SELECT id,email_status FROM wishes WHERE id=?").bind(p.id).first();if(existing)return json({ok:true,emailPending:existing.email_status!=="accepted"});const ip=request.headers.get("cf-connecting-ip")||"unknown";const hash=Array.from(new Uint8Array(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(ip+new Date().toISOString().slice(0,10))))).map(x=>x.toString(16).padStart(2,"0")).join("");const now=Date.now();const result=await db.prepare("INSERT INTO wishes (id,name,message,created_at,email_status,ip_hash) SELECT ?,?,?,?,'pending',? WHERE NOT EXISTS (SELECT 1 FROM wishes WHERE ip_hash=? AND created_at>?) ON CONFLICT(id) DO NOTHING").bind(p.id,name,message,now,hash,hash,now-30000).run();if(!result.meta.changes)return json({error:"Please wait 30 seconds before leaving another message."},429);const accepted=await forwardEmail(name,message,p.id,request.url);if(accepted){try{await db.prepare("UPDATE wishes SET email_status='accepted' WHERE id=?").bind(p.id).run()}catch{console.error("Email status update failed")}}return json({ok:true,emailPending:!accepted},201)}catch(error){console.error("Guestbook save failed",error);return json({error:"We couldn’t save your message. Please try again; your words are still here."},503)}}
